name: Push
"on":
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch: {}
jobs:
  ci:
    name: CI
    run: .action-server/tasks/ci.sh
    timeoutMinutes: 120
