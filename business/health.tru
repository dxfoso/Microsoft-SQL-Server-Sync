version 0.3

route GET /call/health
function health(): map<json> {
  return { ok: true, generatedAt: date.now() };
}

route GET /call/ready
function ready(): map<json> {
  return { ok: true, generatedAt: date.now() };
}
