version 0.3

namespace sql_sync

route GET /call/sql-sync/info
function info(): map<json> {
  return {
    status: "active",
    backend: "tru",
    message: "SQL sync control-plane logic is served from the root business directory."
  };
}
