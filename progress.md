95% complete - Separate SQL clients on one Windows device are implemented, regression-tested, published in Windows client 1.0.235+239, and supported by the deployed server. The original client and each added instance have independent identity, state, Change Tracking cursors, logs, supervisor, Startup shortcut, and update target. The full Standard gate and 24-scenario three-client Docker suite pass. Remaining: let `alshallan2` install the pending update, then use **Settings > Add separate SQL client** on that machine for the second SQL endpoint and confirm both identities are online. Real synchronization remains paused until both endpoints and table policies are verified.
