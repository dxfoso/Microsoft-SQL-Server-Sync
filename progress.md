70% complete - Latest-change-wins is implemented for primary-key and SQL unique-key conflicts, with atomic scoped replacement and no table-wide replacement. Client, frontend, contract, and the 24-scenario three-client Docker tests pass. Remaining: finish chaos/version-matrix tests, publish client 1.0.231+235, deploy immutable server images, verify clients, then run one live sync only if before 07:55 local time.
