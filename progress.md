95% complete - Resumable typed SQL bulk staging is implemented for comparison and apply, with safe literal-SQL fallback. The 14,030-row local benchmark is 3.12x faster, and all Standard atomic/recovery/Docker tests pass. Remaining: publish Windows client `1.0.266+270`, deploy the immutable release, and verify the live manifest/startup without starting a production sync.
